---
import Icon from '../common/Icon.astro';

const navLinks = [
  { label: "ABOUT", href: "#about" },
  { label: "INFRA", href: "#infrastructure" },
  { label: "PROJECTS", href: "#projects" },
  { label: "CI/CD", href: "#pipeline" },
  { label: "STACK", href: "#stack" },
  { label: "EXPERIENCE", href: "#experience" },
  { label: "CONTACT", href: "#contact" }
];
---

<header id="main-navbar" class="navbar">
  <div class="container navbar-container">
    <!-- Brand Logo with micro pulse status dot -->
    <a href="#" class="brand-logo" aria-label="Prasaswo Tepe Portfolio Home">
      <span class="status-indicator-dot" title="Systems Operational" aria-hidden="true"></span>
      <span class="brand-bracket font-mono">&lt;&gt;</span>
      <span class="brand-name font-mono">PRASASWO.TEPE</span>
    </a>

    <!-- Desktop Nav Links -->
    <nav class="desktop-nav" aria-label="Main Navigation">
      <ul class="nav-list">
        {navLinks.map((link) => (
          <li>
            <a href={link.href} class="nav-link font-mono">
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- Right Controls: Compact Cmd+K Search -->
    <div class="navbar-actions">
      <button
        type="button"
        id="cmd-palette-btn"
        class="cmd-trigger-btn"
        aria-label="Open command palette (Ctrl+K or Cmd+K)"
        onclick="window.openCommandPalette && window.openCommandPalette()"
      >
        <Icon name="search" size={13} />
        <kbd class="cmd-kbd font-mono">⌘K</kbd>
      </button>
    </div>
  </div>
</header>

<style>
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    width: 100%;
    height: 60px;
    z-index: var(--z-sticky-nav);
    background: rgba(6, 11, 18, 0.85);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(27, 48, 66, 0.65);
    transition: 
      top 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      width 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      max-width 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      height 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      border-radius 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      border-color 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      background 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      box-shadow 0.35s cubic-bezier(0.16, 1, 0.3, 1),
      padding 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .navbar.scrolled {
    background: rgba(6, 11, 18, 0.94);
    border-bottom-color: var(--color-border);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.45);
  }

  /* Desktop floating pill style & transition */
  @media (min-width: 900px) {
    .navbar {
      top: 18px;
      left: 50%;
      right: auto;
      transform: translateX(-50%);
      width: min(calc(100% - 48px), 960px);
      height: 54px;
      border-radius: var(--radius-pill);
      border: 1px solid rgba(25, 211, 230, 0.28);
      background: linear-gradient(180deg, rgba(20, 33, 49, 0.88) 0%, rgba(10, 18, 28, 0.82) 100%);
      backdrop-filter: blur(20px) saturate(160%);
      -webkit-backdrop-filter: blur(20px) saturate(160%);
      box-shadow: 
        inset 0 1px 0 0 rgba(255, 255, 255, 0.18),
        inset 0 0 16px rgba(25, 211, 230, 0.06),
        0 14px 40px rgba(0, 0, 0, 0.75),
        0 0 28px rgba(25, 211, 230, 0.12);
      padding-inline: var(--space-5);
    }

    .navbar .navbar-container {
      width: 100%;
      max-width: 100%;
      padding-inline: 0;
    }

    .navbar.scrolled {
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 100%;
      max-width: 100%;
      height: 64px;
      border-radius: 0;
      border-top: none;
      border-left: none;
      border-right: none;
      border-bottom: 1px solid rgba(27, 48, 66, 0.85);
      background: rgba(6, 11, 18, 0.90);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      box-shadow: 0 4px 24px rgba(0, 0, 0, 0.55);
      padding-inline: 0;
    }

    .navbar.scrolled .navbar-container {
      width: min(calc(100% - 64px), 1420px);
      margin-inline: auto;
      padding-inline: 0;
    }
  }

  .navbar-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
    transition: max-width 0.35s cubic-bezier(0.16, 1, 0.3, 1), padding 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .brand-logo {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-text-primary);
    text-decoration: none;
    letter-spacing: 0.05em;
  }

  .status-indicator-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-success);
    box-shadow: 0 0 8px rgba(32, 211, 155, 0.8);
    display: inline-block;
    flex-shrink: 0;
    animation: status-pulse 3s infinite ease-in-out;
  }

  @keyframes status-pulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.5;
      transform: scale(0.85);
    }
  }

  .brand-bracket {
    color: var(--color-accent-primary);
  }

  .brand-name {
    transition: color var(--duration-fast);
  }

  .brand-logo:hover .brand-name {
    color: var(--color-accent-primary);
  }

  /* Desktop Nav */
  .desktop-nav {
    display: none;
  }

  @media (min-width: 900px) {
    .desktop-nav {
      display: block;
    }
  }

  .nav-list {
    display: flex;
    align-items: center;
    gap: clamp(14px, 1.8vw, 22px);
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    font-size: var(--text-xs);
    font-weight: 600;
    color: var(--color-text-secondary);
    text-decoration: none;
    padding-block: var(--space-2);
    position: relative;
    transition: color var(--duration-fast);
    letter-spacing: 0.06em;
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--color-accent-primary);
  }

  .nav-link::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: var(--color-accent-primary);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform var(--duration-fast) var(--ease-standard);
  }

  .nav-link:hover::after,
  .nav-link.active::after {
    transform: scaleX(1);
  }

  /* Actions */
  .navbar-actions {
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  .cmd-trigger-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    height: 32px;
    padding: 0 10px;
    background: rgba(16, 26, 38, 0.6);
    border: 1px solid rgba(27, 48, 66, 0.85);
    border-radius: var(--radius-sm);
    color: var(--color-text-secondary);
    font-size: var(--text-xs);
    cursor: pointer;
    transition: all var(--duration-fast);
    outline: none;
  }

  .cmd-trigger-btn:hover {
    border-color: var(--color-accent-primary);
    color: var(--color-accent-primary);
    background: var(--color-surface-2);
    box-shadow: 0 0 12px rgba(25, 211, 230, 0.15);
  }

  .cmd-trigger-btn:focus-visible {
    outline: 2px solid var(--color-accent-primary);
    outline-offset: 2px;
  }

  .cmd-kbd {
    background: var(--color-bg-0);
    border: 1px solid rgba(27, 48, 66, 0.8);
    border-radius: 3px;
    padding: 1px 4px;
    font-size: 0.65rem;
    color: var(--color-text-muted);
    line-height: 1.2;
  }

  @media (max-width: 480px) {
    .navbar {
      height: 56px;
    }
    .brand-logo {
      font-size: 0.82rem;
      gap: 5px;
    }
    .cmd-trigger-btn {
      height: 30px;
      padding: 0 8px;
    }
  }

  @media (max-width: 360px) {
    .brand-logo {
      font-size: 0.74rem;
      gap: 4px;
    }
    .cmd-kbd {
      display: none;
    }
    .cmd-trigger-btn {
      padding: 0 6px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .status-indicator-dot {
      animation: none;
    }
  }
</style>
